verify that : condition
	raise 403 Forbidden if False
define : value ; expression
	replace all the instance of value by value of expression
exec : python
    execute the python
add header : name ; expression
    add a header to the http response
set http code : code
    respond to the client with this code (à mettre en dernier !!!)

sign function(bytes) -> str signature
path bytes : path url jusquà ?
parameters dict url après ?
headers dict http headers
url bytes full url
ip_srv str adresse ip du serveur
ip_cli str adresse ip du client
get_voyage function(int x) -> no_voyage du robot x
reset_screen function(int x) -> None reset l'écran du robot x
command_drink(int x,int y) -> None commande la boisson y au robot x